环境问题
    sysinfo会重启
    pwm callback 可以设置，但是调用会重启
    scanf用hhx无效
    irq里面连usart_print_async都不行，更别说普通的，直接重启，只能用 LUAT_DEBUG_PRINT
    下载经常失败：猜测：能deinit的都deinit后，可以直接下载，deinit也可以为ota准备

    gpio13不输出
    软件定时器不准确
    gpio12\13和uart冲突，会造成20秒一次重启，具体uart未定

需要解决
    重启一次D 增加1
